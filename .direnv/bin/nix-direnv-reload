#!/usr/bin/env bash
set -e
if [[ ! -d "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust/.envrc" "/home/ardit/repos/Arbeit/KMW-FB_Rust/src_rust/.direnv"/*.rc
